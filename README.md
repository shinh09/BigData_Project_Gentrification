(가상환경 활성화 선택사항)
py -m venv .venv
.venv\Scripts\activate

(설치 !!)
- py -m pip install --upgrade pip
- py -m pip install pandas selenium "selenium-wire<5" undetected-chromedriver requests beautifulsoup4 lxml python-dateutil "blinker<2"

py step1_collect_links.py
